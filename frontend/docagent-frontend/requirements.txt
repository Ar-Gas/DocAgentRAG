# Python dependencies for DocAgent frontend
# These are optional dependencies for development, testing, and backend integration

# HTTP client for API testing
requests==2.32.3

# Testing framework
pytest==8.3.2
pytest-cov==5.0.0

# Environment variable management
python-dotenv==1.0.0

# Code quality tools
flake8==7.1.1
black==24.8.0

# Optional: For generating mock data
faker==24.4.0

# Optional: For static analysis
mypy==1.11.2

# Optional: For documentation
sphinx==7.3.7

# 配置环境经验：
# 1. 前端项目需要 Node.js 20.19.0 或更高版本
# 2. 安装 Node.js 的推荐方法：
#    curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
#    sudo apt-get install -y nodejs
# 3. 确保 npm 版本是最新的：npm install -g npm
# 4. 前端项目需要配置 vite.config.js 文件，设置路径别名
# 5. 如果遇到依赖解析错误，检查 vite.config.js 文件中的配置
# 6. 前端服务默认运行在 http://localhost:5173/
